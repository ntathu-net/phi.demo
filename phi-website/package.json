{
  "name": "phi-website",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir dist && cp -r views dist/ && cp -r public dist/ && cp netlify.toml dist/ && cp -r netlify dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless-http": "^4.0.0"
  }
}
